{
  "name": "replay-akadera",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "deploy": "sam build && sam deploy"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  }
}
