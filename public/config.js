// API base URL — change this after SAM deployment.
// Local dev: leave empty (calls relative /api/songs on localhost:3000).
// Production: set to your API Gateway URL from `sam deploy` output.
const API_BASE = 'https://x2k8ycadd1.execute-api.eu-central-1.amazonaws.com';
